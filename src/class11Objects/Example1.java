package class11;

public class Example1 {
    public static void main(String[] args) {

      /* int[][] matrix = {{10, 20, 30},
                {45, 55, 66},
                {30, 40, 20, 10, 25}
        };
        System.out.println(matrix[2][4]);
        System.out.println(Arrays.toString(matrix[0]));    // to print 0,0    0,1    0,2
        for (int i = 0; i < matrix[0].length; i++) {
            System.out.println(matrix[0][1]);
        }
        int[] arr = matrix[0]; //getting complete first array from 2D array
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
        int[][] matrix;

        //to print all elements
        for (i = 0, i < matrix.length; i++){         // tells us how many 1D arrays are present in this 2D array
            for (j = 0, j < matrix[i].length; j++){     // bring the size of each 1D array (columns)
                System.out.println(matrix[i][j] + " ");
            }
            System.out.println();

        }
        // print odd numbers
        for (i = 0; i < matrix.length; i++) {  // tells us how many 1D arrays are present in this 2D array
            for (j = 0; j < matrix[i].length; j++) {
                if (matrix[i][j] % 2 != 0) {
                    System.out.println(matrix[i][j] + ' ');
                }
                System.out.println();
            }
            //Inhanced for loop Q1 on project
            for (int[] arr : matrix) {

                for (int number : arr) {
                    System.out.println(number + " ");
                }
                System.out.println();
            }

           // project 6
                int num1=10;
            int num2=20;

        }
       // fibonacci num   0 1 1 2 3 5
        // prime no   divided by 1 and the ame num  ex  1  3  5  7  11  13  17*/
    }

}






