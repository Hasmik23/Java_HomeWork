package class15;

public class Dog {
    public static void main(String[] args) {

        /*where we can access variables in methods, classes, static
        3 types of variables
        -local inside{} method, loop, and if condition.
          Upside local variable occupy the memory for the shortest time of period. Will be destroyed as soon as done executing.
          Downside local variables can not be accessed outside the log in which they are created. Have very limited scope.
         -instance variable. created inside a class but outside the method.string color; String name;
         Upside we can use them in instance methods of that class. their scope are larger than local variables
        -Static variables :associate with word static. inside cl;ass but outside the methods. static int noOfFlags=4

        Instance
        */

    }
}
